---
# cf0 OpenSpace Playbook
# Deploys: OpenSpace dashboard and MCP server

- name: Deploy cf0 OpenSpace
  hosts: cf0
  become: true
  gather_facts: false

  roles:
    - role: openspace
      tags: [openspace]
